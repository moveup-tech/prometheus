import { Body, BodyProps } from "./body";
import { Root, RootProps } from "./root";
import { Header, HeaderProps } from "./header";
import { Footer, CardFooterProps } from "./footer";

export const Card = {
  Root,
  Header,
  Body,
  Footer,
};

export type { RootProps, HeaderProps, BodyProps, CardFooterProps };
